DATABASE_URL="postgresql://postgres:escape123@localhost:5434/escape_db"
ADMIN_KEY="change-me-in-production"
# Comma-separated list of allowed CORS origins, or * for all
ALLOWED_ORIGINS="*"
# Max requests per session before lockout
SESSION_REQUEST_LIMIT=150
